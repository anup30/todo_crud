//////////////////////////////////////////////////////////////////////////////// todo_card.dart

import 'package:flutter/material.dart';
//import 'main.dart';
class TodoCard extends StatelessWidget {
  const TodoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // return Card(implemented in this page); under ListView.builder, in main page // at video 53:00, it's a mess!
  }
}


